
            <div class="navbar">
              <div class="navbar-inner">
                <form class="navbar-search pull-right">
                  <%= search_form_for @q, :url => search_path,
                        :html => {:method => :post} , :class=>"navbar-search pull-right" do |f| %>
                        <%= f.text_field :title_cont, :class =>"search-query", :placeholder =>"Search" %>                        
                        <% end %>

                </form>
              </div>
            </div>



<div class="row-fluid">
            <ul class="thumbnails">
<% @coffeecups.each do |coffeecup| %>
              <li class="span4">
                <div class="thumbnail">
                  <img style="float:left;padding 5px"  src="<%= coffeecup.img %>">
                  <div class="caption" >
                    <h4 ><%= coffeecup.title %></h4>
                    <p><%= coffeecup.short %></p>
                    <p><a href="#" class="btn btn-primary">Demo</a> <a href="#" class="btn">About</a></p>
                  </div>
                </div>
              </li>
              <% end %>        
             
              
            </ul>
          </div>

                <%= paginate @coffeecups %>

<%= render_cell :coffee, :recommended %>